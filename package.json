{
  "name": "cosplay-map-server",
  "version": "0.0.0",
  "private": true,
  "description": "Backend microservices for the Helsinki/Vantaa cosplay map (Cloudflare Workers + D1)",
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "dev": "concurrently -k -n api,gis -c magenta,yellow \"pnpm --filter api-service exec wrangler dev --port 8787\" \"pnpm --filter gis-proxy exec wrangler dev --port 8789\"",
    "migrate": "pnpm --filter api-service db:migrate",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.5.0"
  }
}
