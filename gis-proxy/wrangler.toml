name = "gis-proxy"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[vars]
# Optional upstream WFS/GeoJSON endpoint from hel.kartta.fi / Helsinki Region Infoshare.
# If empty or unreachable, the bundled fallback FeatureCollection is served instead.
HEL_WFS_URL = ""

[observability]
enabled = true
